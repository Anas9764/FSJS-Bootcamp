let myStr = "   FSJS   ";
let trimmed = myStr.trim();
console.log(`Before trim: ${myStr}, ${myStr.length}`);
console.log(`After trim: ${trimmed}, ${trimmed.length}`);

// output
// Before trim:    FSJS   , 10
// After trim: FSJS, 4